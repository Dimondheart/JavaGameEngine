package main;

/** Manages custom user settings. */
public class Settings
{
	/** If all graphics should be loaded on startup. */
	public static synchronized boolean preloadGraphics()
	{
		return true;
	}
}
