/** All background music tracks. This package is provided as a common way
 * to sort sound files, and is not necessary to keep if developers want to
 * organize sound files differently (as you still have to specify bgm/soundfile.xyz
 * because it is relative to the ~/resources/sounds folder).
 */
package game.resources.sounds.bgm;